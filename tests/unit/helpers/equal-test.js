
import { equal } from 'dummy/helpers/equal';
import { module, test } from 'qunit';

module('Unit | Helper | equal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equal([42]);
  assert.ok(result);
});

