import string from 'dummy/utils/string';
import { module, test } from 'qunit';

module('Unit | Utility | string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = string;
  assert.ok(result);
});
