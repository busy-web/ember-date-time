import keyHandler from 'dummy/utils/key-handler';
import { module, test } from 'qunit';

module('Unit | Utility | key handler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keyHandler();
  assert.ok(result);
});
