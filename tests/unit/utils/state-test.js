import state from 'dummy/utils/state';
import { module, test } from 'qunit';

module('Unit | Utility | state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = state;
  assert.ok(result);
});
