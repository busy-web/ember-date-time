import clockData from 'dummy/utils/clock/data';
import { module, test } from 'qunit';

module('Unit | Utility | clock/data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockData;
  assert.ok(result);
});
