import clockString from 'dummy/utils/clock/string';
import { module, test } from 'qunit';

module('Unit | Utility | clock/string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockString();
  assert.ok(result);
});
