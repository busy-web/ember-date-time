import clockBase from 'dummy/utils/clock/base';
import { module, test } from 'qunit';

module('Unit | Utility | clock/base');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockBase;
  assert.ok(result);
});
