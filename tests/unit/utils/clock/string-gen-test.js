import clockStringGen from 'dummy/utils/clock/string-gen';
import { module, test } from 'qunit';

module('Unit | Utility | clock/string gen');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockStringGen();
  assert.ok(result);
});
