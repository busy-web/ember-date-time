import clockSvg from 'dummy/utils/clock/svg';
import { module, test } from 'qunit';

module('Unit | Utility | clock/svg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockSvg();
  assert.ok(result);
});
