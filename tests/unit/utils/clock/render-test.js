import clockRender from 'dummy/utils/clock/render';
import { module, test } from 'qunit';

module('Unit | Utility | clock/render');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clockRender;
  assert.ok(result);
});
