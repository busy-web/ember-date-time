import timePicker from 'dummy/utils/time-picker';
import { module, test } from 'qunit';

module('Unit | Utility | time picker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timePicker();
  assert.ok(result);
});
