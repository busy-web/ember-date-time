import paperTime from 'dummy/utils/paper-time';
import { module, test } from 'qunit';

module('Unit | Utility | paper time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paperTime();
  assert.ok(result);
});
