import paperDate from 'dummy/utils/paper-date';
import { module, test } from 'qunit';

module('Unit | Utility | paper date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paperDate;
  assert.ok(result);
});
