import clock from 'dummy/utils/clock';
import { module, test } from 'qunit';

module('Unit | Utility | clock');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clock();
  assert.ok(result);
});
