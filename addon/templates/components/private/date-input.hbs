
<input type="{{type}}" min="{{min}}" max="{{max}}" value="{{value}}" onclick={{action "clickEvent"}} onfocus={{action "focusInEvent"}} onblur={{action "focusOutEvent"}} onkeydown={{action "keyDown"}}/>
